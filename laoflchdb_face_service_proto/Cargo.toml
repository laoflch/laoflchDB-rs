[package]
name = "laoflchdb_face_service_proto"
version = "0.1.0"
edition = "2021"
description = "Proto definitions for laoflchdb FaceService (detection + ArcFace feature extraction + vector indexing)"

[dependencies]
tonic = "0.11"
prost = "0.12"

[build-dependencies]
tonic-build = "0.11"
